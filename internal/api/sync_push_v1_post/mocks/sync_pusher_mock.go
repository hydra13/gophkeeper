// Code generated by http://github.com/gojuno/minimock (v3.4.7). DO NOT EDIT.

package mocks

import (
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock/v3"
	mm_sync_push_v1_post "github.com/hydra13/gophkeeper/internal/api/sync_push_v1_post"
	"github.com/hydra13/gophkeeper/internal/models"
)

// SyncPusherMock implements mm_sync_push_v1_post.SyncPusher
type SyncPusherMock struct {
	t          minimock.Tester
	finishOnce sync.Once

	funcPush          func(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange) (ra1 []models.RecordRevision, sa1 []models.SyncConflict, err error)
	funcPushOrigin    string
	inspectFuncPush   func(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange)
	afterPushCounter  uint64
	beforePushCounter uint64
	PushMock          mSyncPusherMockPush
}

// NewSyncPusherMock returns a mock for mm_sync_push_v1_post.SyncPusher
func NewSyncPusherMock(t minimock.Tester) *SyncPusherMock {
	m := &SyncPusherMock{t: t}

	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.PushMock = mSyncPusherMockPush{mock: m}
	m.PushMock.callArgs = []*SyncPusherMockPushParams{}

	t.Cleanup(m.MinimockFinish)

	return m
}

type mSyncPusherMockPush struct {
	optional           bool
	mock               *SyncPusherMock
	defaultExpectation *SyncPusherMockPushExpectation
	expectations       []*SyncPusherMockPushExpectation

	callArgs []*SyncPusherMockPushParams
	mutex    sync.RWMutex

	expectedInvocations       uint64
	expectedInvocationsOrigin string
}

// SyncPusherMockPushExpectation specifies expectation struct of the SyncPusher.Push
type SyncPusherMockPushExpectation struct {
	mock               *SyncPusherMock
	params             *SyncPusherMockPushParams
	paramPtrs          *SyncPusherMockPushParamPtrs
	expectationOrigins SyncPusherMockPushExpectationOrigins
	results            *SyncPusherMockPushResults
	returnOrigin       string
	Counter            uint64
}

// SyncPusherMockPushParams contains parameters of the SyncPusher.Push
type SyncPusherMockPushParams struct {
	userID   int64
	deviceID string
	changes  []mm_sync_push_v1_post.PendingChange
}

// SyncPusherMockPushParamPtrs contains pointers to parameters of the SyncPusher.Push
type SyncPusherMockPushParamPtrs struct {
	userID   *int64
	deviceID *string
	changes  *[]mm_sync_push_v1_post.PendingChange
}

// SyncPusherMockPushResults contains results of the SyncPusher.Push
type SyncPusherMockPushResults struct {
	ra1 []models.RecordRevision
	sa1 []models.SyncConflict
	err error
}

// SyncPusherMockPushOrigins contains origins of expectations of the SyncPusher.Push
type SyncPusherMockPushExpectationOrigins struct {
	origin         string
	originUserID   string
	originDeviceID string
	originChanges  string
}

// Marks this method to be optional. The default behavior of any method with Return() is '1 or more', meaning
// the test will fail minimock's automatic final call check if the mocked method was not called at least once.
// Optional() makes method check to work in '0 or more' mode.
// It is NOT RECOMMENDED to use this option unless you really need it, as default behaviour helps to
// catch the problems when the expected method call is totally skipped during test run.
func (mmPush *mSyncPusherMockPush) Optional() *mSyncPusherMockPush {
	mmPush.optional = true
	return mmPush
}

// Expect sets up expected params for SyncPusher.Push
func (mmPush *mSyncPusherMockPush) Expect(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange) *mSyncPusherMockPush {
	if mmPush.mock.funcPush != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Set")
	}

	if mmPush.defaultExpectation == nil {
		mmPush.defaultExpectation = &SyncPusherMockPushExpectation{}
	}

	if mmPush.defaultExpectation.paramPtrs != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by ExpectParams functions")
	}

	mmPush.defaultExpectation.params = &SyncPusherMockPushParams{userID, deviceID, changes}
	mmPush.defaultExpectation.expectationOrigins.origin = minimock.CallerInfo(1)
	for _, e := range mmPush.expectations {
		if minimock.Equal(e.params, mmPush.defaultExpectation.params) {
			mmPush.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmPush.defaultExpectation.params)
		}
	}

	return mmPush
}

// ExpectUserIDParam1 sets up expected param userID for SyncPusher.Push
func (mmPush *mSyncPusherMockPush) ExpectUserIDParam1(userID int64) *mSyncPusherMockPush {
	if mmPush.mock.funcPush != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Set")
	}

	if mmPush.defaultExpectation == nil {
		mmPush.defaultExpectation = &SyncPusherMockPushExpectation{}
	}

	if mmPush.defaultExpectation.params != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Expect")
	}

	if mmPush.defaultExpectation.paramPtrs == nil {
		mmPush.defaultExpectation.paramPtrs = &SyncPusherMockPushParamPtrs{}
	}
	mmPush.defaultExpectation.paramPtrs.userID = &userID
	mmPush.defaultExpectation.expectationOrigins.originUserID = minimock.CallerInfo(1)

	return mmPush
}

// ExpectDeviceIDParam2 sets up expected param deviceID for SyncPusher.Push
func (mmPush *mSyncPusherMockPush) ExpectDeviceIDParam2(deviceID string) *mSyncPusherMockPush {
	if mmPush.mock.funcPush != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Set")
	}

	if mmPush.defaultExpectation == nil {
		mmPush.defaultExpectation = &SyncPusherMockPushExpectation{}
	}

	if mmPush.defaultExpectation.params != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Expect")
	}

	if mmPush.defaultExpectation.paramPtrs == nil {
		mmPush.defaultExpectation.paramPtrs = &SyncPusherMockPushParamPtrs{}
	}
	mmPush.defaultExpectation.paramPtrs.deviceID = &deviceID
	mmPush.defaultExpectation.expectationOrigins.originDeviceID = minimock.CallerInfo(1)

	return mmPush
}

// ExpectChangesParam3 sets up expected param changes for SyncPusher.Push
func (mmPush *mSyncPusherMockPush) ExpectChangesParam3(changes []mm_sync_push_v1_post.PendingChange) *mSyncPusherMockPush {
	if mmPush.mock.funcPush != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Set")
	}

	if mmPush.defaultExpectation == nil {
		mmPush.defaultExpectation = &SyncPusherMockPushExpectation{}
	}

	if mmPush.defaultExpectation.params != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Expect")
	}

	if mmPush.defaultExpectation.paramPtrs == nil {
		mmPush.defaultExpectation.paramPtrs = &SyncPusherMockPushParamPtrs{}
	}
	mmPush.defaultExpectation.paramPtrs.changes = &changes
	mmPush.defaultExpectation.expectationOrigins.originChanges = minimock.CallerInfo(1)

	return mmPush
}

// Inspect accepts an inspector function that has same arguments as the SyncPusher.Push
func (mmPush *mSyncPusherMockPush) Inspect(f func(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange)) *mSyncPusherMockPush {
	if mmPush.mock.inspectFuncPush != nil {
		mmPush.mock.t.Fatalf("Inspect function is already set for SyncPusherMock.Push")
	}

	mmPush.mock.inspectFuncPush = f

	return mmPush
}

// Return sets up results that will be returned by SyncPusher.Push
func (mmPush *mSyncPusherMockPush) Return(ra1 []models.RecordRevision, sa1 []models.SyncConflict, err error) *SyncPusherMock {
	if mmPush.mock.funcPush != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Set")
	}

	if mmPush.defaultExpectation == nil {
		mmPush.defaultExpectation = &SyncPusherMockPushExpectation{mock: mmPush.mock}
	}
	mmPush.defaultExpectation.results = &SyncPusherMockPushResults{ra1, sa1, err}
	mmPush.defaultExpectation.returnOrigin = minimock.CallerInfo(1)
	return mmPush.mock
}

// Set uses given function f to mock the SyncPusher.Push method
func (mmPush *mSyncPusherMockPush) Set(f func(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange) (ra1 []models.RecordRevision, sa1 []models.SyncConflict, err error)) *SyncPusherMock {
	if mmPush.defaultExpectation != nil {
		mmPush.mock.t.Fatalf("Default expectation is already set for the SyncPusher.Push method")
	}

	if len(mmPush.expectations) > 0 {
		mmPush.mock.t.Fatalf("Some expectations are already set for the SyncPusher.Push method")
	}

	mmPush.mock.funcPush = f
	mmPush.mock.funcPushOrigin = minimock.CallerInfo(1)
	return mmPush.mock
}

// When sets expectation for the SyncPusher.Push which will trigger the result defined by the following
// Then helper
func (mmPush *mSyncPusherMockPush) When(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange) *SyncPusherMockPushExpectation {
	if mmPush.mock.funcPush != nil {
		mmPush.mock.t.Fatalf("SyncPusherMock.Push mock is already set by Set")
	}

	expectation := &SyncPusherMockPushExpectation{
		mock:               mmPush.mock,
		params:             &SyncPusherMockPushParams{userID, deviceID, changes},
		expectationOrigins: SyncPusherMockPushExpectationOrigins{origin: minimock.CallerInfo(1)},
	}
	mmPush.expectations = append(mmPush.expectations, expectation)
	return expectation
}

// Then sets up SyncPusher.Push return parameters for the expectation previously defined by the When method
func (e *SyncPusherMockPushExpectation) Then(ra1 []models.RecordRevision, sa1 []models.SyncConflict, err error) *SyncPusherMock {
	e.results = &SyncPusherMockPushResults{ra1, sa1, err}
	return e.mock
}

// Times sets number of times SyncPusher.Push should be invoked
func (mmPush *mSyncPusherMockPush) Times(n uint64) *mSyncPusherMockPush {
	if n == 0 {
		mmPush.mock.t.Fatalf("Times of SyncPusherMock.Push mock can not be zero")
	}
	mm_atomic.StoreUint64(&mmPush.expectedInvocations, n)
	mmPush.expectedInvocationsOrigin = minimock.CallerInfo(1)
	return mmPush
}

func (mmPush *mSyncPusherMockPush) invocationsDone() bool {
	if len(mmPush.expectations) == 0 && mmPush.defaultExpectation == nil && mmPush.mock.funcPush == nil {
		return true
	}

	totalInvocations := mm_atomic.LoadUint64(&mmPush.mock.afterPushCounter)
	expectedInvocations := mm_atomic.LoadUint64(&mmPush.expectedInvocations)

	return totalInvocations > 0 && (expectedInvocations == 0 || expectedInvocations == totalInvocations)
}

// Push implements mm_sync_push_v1_post.SyncPusher
func (mmPush *SyncPusherMock) Push(userID int64, deviceID string, changes []mm_sync_push_v1_post.PendingChange) (ra1 []models.RecordRevision, sa1 []models.SyncConflict, err error) {
	mm_atomic.AddUint64(&mmPush.beforePushCounter, 1)
	defer mm_atomic.AddUint64(&mmPush.afterPushCounter, 1)

	mmPush.t.Helper()

	if mmPush.inspectFuncPush != nil {
		mmPush.inspectFuncPush(userID, deviceID, changes)
	}

	mm_params := SyncPusherMockPushParams{userID, deviceID, changes}

	// Record call args
	mmPush.PushMock.mutex.Lock()
	mmPush.PushMock.callArgs = append(mmPush.PushMock.callArgs, &mm_params)
	mmPush.PushMock.mutex.Unlock()

	for _, e := range mmPush.PushMock.expectations {
		if minimock.Equal(*e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.ra1, e.results.sa1, e.results.err
		}
	}

	if mmPush.PushMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmPush.PushMock.defaultExpectation.Counter, 1)
		mm_want := mmPush.PushMock.defaultExpectation.params
		mm_want_ptrs := mmPush.PushMock.defaultExpectation.paramPtrs

		mm_got := SyncPusherMockPushParams{userID, deviceID, changes}

		if mm_want_ptrs != nil {

			if mm_want_ptrs.userID != nil && !minimock.Equal(*mm_want_ptrs.userID, mm_got.userID) {
				mmPush.t.Errorf("SyncPusherMock.Push got unexpected parameter userID, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmPush.PushMock.defaultExpectation.expectationOrigins.originUserID, *mm_want_ptrs.userID, mm_got.userID, minimock.Diff(*mm_want_ptrs.userID, mm_got.userID))
			}

			if mm_want_ptrs.deviceID != nil && !minimock.Equal(*mm_want_ptrs.deviceID, mm_got.deviceID) {
				mmPush.t.Errorf("SyncPusherMock.Push got unexpected parameter deviceID, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmPush.PushMock.defaultExpectation.expectationOrigins.originDeviceID, *mm_want_ptrs.deviceID, mm_got.deviceID, minimock.Diff(*mm_want_ptrs.deviceID, mm_got.deviceID))
			}

			if mm_want_ptrs.changes != nil && !minimock.Equal(*mm_want_ptrs.changes, mm_got.changes) {
				mmPush.t.Errorf("SyncPusherMock.Push got unexpected parameter changes, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmPush.PushMock.defaultExpectation.expectationOrigins.originChanges, *mm_want_ptrs.changes, mm_got.changes, minimock.Diff(*mm_want_ptrs.changes, mm_got.changes))
			}

		} else if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmPush.t.Errorf("SyncPusherMock.Push got unexpected parameters, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
				mmPush.PushMock.defaultExpectation.expectationOrigins.origin, *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		mm_results := mmPush.PushMock.defaultExpectation.results
		if mm_results == nil {
			mmPush.t.Fatal("No results are set for the SyncPusherMock.Push")
		}
		return (*mm_results).ra1, (*mm_results).sa1, (*mm_results).err
	}
	if mmPush.funcPush != nil {
		return mmPush.funcPush(userID, deviceID, changes)
	}
	mmPush.t.Fatalf("Unexpected call to SyncPusherMock.Push. %v %v %v", userID, deviceID, changes)
	return
}

// PushAfterCounter returns a count of finished SyncPusherMock.Push invocations
func (mmPush *SyncPusherMock) PushAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmPush.afterPushCounter)
}

// PushBeforeCounter returns a count of SyncPusherMock.Push invocations
func (mmPush *SyncPusherMock) PushBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmPush.beforePushCounter)
}

// Calls returns a list of arguments used in each call to SyncPusherMock.Push.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmPush *mSyncPusherMockPush) Calls() []*SyncPusherMockPushParams {
	mmPush.mutex.RLock()

	argCopy := make([]*SyncPusherMockPushParams, len(mmPush.callArgs))
	copy(argCopy, mmPush.callArgs)

	mmPush.mutex.RUnlock()

	return argCopy
}

// MinimockPushDone returns true if the count of the Push invocations corresponds
// the number of defined expectations
func (m *SyncPusherMock) MinimockPushDone() bool {
	if m.PushMock.optional {
		// Optional methods provide '0 or more' call count restriction.
		return true
	}

	for _, e := range m.PushMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	return m.PushMock.invocationsDone()
}

// MinimockPushInspect logs each unmet expectation
func (m *SyncPusherMock) MinimockPushInspect() {
	for _, e := range m.PushMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to SyncPusherMock.Push at\n%s with params: %#v", e.expectationOrigins.origin, *e.params)
		}
	}

	afterPushCounter := mm_atomic.LoadUint64(&m.afterPushCounter)
	// if default expectation was set then invocations count should be greater than zero
	if m.PushMock.defaultExpectation != nil && afterPushCounter < 1 {
		if m.PushMock.defaultExpectation.params == nil {
			m.t.Errorf("Expected call to SyncPusherMock.Push at\n%s", m.PushMock.defaultExpectation.returnOrigin)
		} else {
			m.t.Errorf("Expected call to SyncPusherMock.Push at\n%s with params: %#v", m.PushMock.defaultExpectation.expectationOrigins.origin, *m.PushMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcPush != nil && afterPushCounter < 1 {
		m.t.Errorf("Expected call to SyncPusherMock.Push at\n%s", m.funcPushOrigin)
	}

	if !m.PushMock.invocationsDone() && afterPushCounter > 0 {
		m.t.Errorf("Expected %d calls to SyncPusherMock.Push at\n%s but found %d calls",
			mm_atomic.LoadUint64(&m.PushMock.expectedInvocations), m.PushMock.expectedInvocationsOrigin, afterPushCounter)
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *SyncPusherMock) MinimockFinish() {
	m.finishOnce.Do(func() {
		if !m.minimockDone() {
			m.MinimockPushInspect()
		}
	})
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *SyncPusherMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *SyncPusherMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockPushDone()
}
