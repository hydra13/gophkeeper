// Code generated by http://github.com/gojuno/minimock (v3.4.7). DO NOT EDIT.

package mocks

import (
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock/v3"
)

// HealthCheckerMock implements mm_health_v1_get.HealthChecker
type HealthCheckerMock struct {
	t          minimock.Tester
	finishOnce sync.Once

	funcHealth          func() (err error)
	funcHealthOrigin    string
	inspectFuncHealth   func()
	afterHealthCounter  uint64
	beforeHealthCounter uint64
	HealthMock          mHealthCheckerMockHealth
}

// NewHealthCheckerMock returns a mock for mm_health_v1_get.HealthChecker
func NewHealthCheckerMock(t minimock.Tester) *HealthCheckerMock {
	m := &HealthCheckerMock{t: t}

	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.HealthMock = mHealthCheckerMockHealth{mock: m}

	t.Cleanup(m.MinimockFinish)

	return m
}

type mHealthCheckerMockHealth struct {
	optional           bool
	mock               *HealthCheckerMock
	defaultExpectation *HealthCheckerMockHealthExpectation
	expectations       []*HealthCheckerMockHealthExpectation

	expectedInvocations       uint64
	expectedInvocationsOrigin string
}

// HealthCheckerMockHealthExpectation specifies expectation struct of the HealthChecker.Health
type HealthCheckerMockHealthExpectation struct {
	mock *HealthCheckerMock

	results      *HealthCheckerMockHealthResults
	returnOrigin string
	Counter      uint64
}

// HealthCheckerMockHealthResults contains results of the HealthChecker.Health
type HealthCheckerMockHealthResults struct {
	err error
}

// Marks this method to be optional. The default behavior of any method with Return() is '1 or more', meaning
// the test will fail minimock's automatic final call check if the mocked method was not called at least once.
// Optional() makes method check to work in '0 or more' mode.
// It is NOT RECOMMENDED to use this option unless you really need it, as default behaviour helps to
// catch the problems when the expected method call is totally skipped during test run.
func (mmHealth *mHealthCheckerMockHealth) Optional() *mHealthCheckerMockHealth {
	mmHealth.optional = true
	return mmHealth
}

// Expect sets up expected params for HealthChecker.Health
func (mmHealth *mHealthCheckerMockHealth) Expect() *mHealthCheckerMockHealth {
	if mmHealth.mock.funcHealth != nil {
		mmHealth.mock.t.Fatalf("HealthCheckerMock.Health mock is already set by Set")
	}

	if mmHealth.defaultExpectation == nil {
		mmHealth.defaultExpectation = &HealthCheckerMockHealthExpectation{}
	}

	return mmHealth
}

// Inspect accepts an inspector function that has same arguments as the HealthChecker.Health
func (mmHealth *mHealthCheckerMockHealth) Inspect(f func()) *mHealthCheckerMockHealth {
	if mmHealth.mock.inspectFuncHealth != nil {
		mmHealth.mock.t.Fatalf("Inspect function is already set for HealthCheckerMock.Health")
	}

	mmHealth.mock.inspectFuncHealth = f

	return mmHealth
}

// Return sets up results that will be returned by HealthChecker.Health
func (mmHealth *mHealthCheckerMockHealth) Return(err error) *HealthCheckerMock {
	if mmHealth.mock.funcHealth != nil {
		mmHealth.mock.t.Fatalf("HealthCheckerMock.Health mock is already set by Set")
	}

	if mmHealth.defaultExpectation == nil {
		mmHealth.defaultExpectation = &HealthCheckerMockHealthExpectation{mock: mmHealth.mock}
	}
	mmHealth.defaultExpectation.results = &HealthCheckerMockHealthResults{err}
	mmHealth.defaultExpectation.returnOrigin = minimock.CallerInfo(1)
	return mmHealth.mock
}

// Set uses given function f to mock the HealthChecker.Health method
func (mmHealth *mHealthCheckerMockHealth) Set(f func() (err error)) *HealthCheckerMock {
	if mmHealth.defaultExpectation != nil {
		mmHealth.mock.t.Fatalf("Default expectation is already set for the HealthChecker.Health method")
	}

	if len(mmHealth.expectations) > 0 {
		mmHealth.mock.t.Fatalf("Some expectations are already set for the HealthChecker.Health method")
	}

	mmHealth.mock.funcHealth = f
	mmHealth.mock.funcHealthOrigin = minimock.CallerInfo(1)
	return mmHealth.mock
}

// Times sets number of times HealthChecker.Health should be invoked
func (mmHealth *mHealthCheckerMockHealth) Times(n uint64) *mHealthCheckerMockHealth {
	if n == 0 {
		mmHealth.mock.t.Fatalf("Times of HealthCheckerMock.Health mock can not be zero")
	}
	mm_atomic.StoreUint64(&mmHealth.expectedInvocations, n)
	mmHealth.expectedInvocationsOrigin = minimock.CallerInfo(1)
	return mmHealth
}

func (mmHealth *mHealthCheckerMockHealth) invocationsDone() bool {
	if len(mmHealth.expectations) == 0 && mmHealth.defaultExpectation == nil && mmHealth.mock.funcHealth == nil {
		return true
	}

	totalInvocations := mm_atomic.LoadUint64(&mmHealth.mock.afterHealthCounter)
	expectedInvocations := mm_atomic.LoadUint64(&mmHealth.expectedInvocations)

	return totalInvocations > 0 && (expectedInvocations == 0 || expectedInvocations == totalInvocations)
}

// Health implements mm_health_v1_get.HealthChecker
func (mmHealth *HealthCheckerMock) Health() (err error) {
	mm_atomic.AddUint64(&mmHealth.beforeHealthCounter, 1)
	defer mm_atomic.AddUint64(&mmHealth.afterHealthCounter, 1)

	mmHealth.t.Helper()

	if mmHealth.inspectFuncHealth != nil {
		mmHealth.inspectFuncHealth()
	}

	if mmHealth.HealthMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmHealth.HealthMock.defaultExpectation.Counter, 1)

		mm_results := mmHealth.HealthMock.defaultExpectation.results
		if mm_results == nil {
			mmHealth.t.Fatal("No results are set for the HealthCheckerMock.Health")
		}
		return (*mm_results).err
	}
	if mmHealth.funcHealth != nil {
		return mmHealth.funcHealth()
	}
	mmHealth.t.Fatalf("Unexpected call to HealthCheckerMock.Health.")
	return
}

// HealthAfterCounter returns a count of finished HealthCheckerMock.Health invocations
func (mmHealth *HealthCheckerMock) HealthAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmHealth.afterHealthCounter)
}

// HealthBeforeCounter returns a count of HealthCheckerMock.Health invocations
func (mmHealth *HealthCheckerMock) HealthBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmHealth.beforeHealthCounter)
}

// MinimockHealthDone returns true if the count of the Health invocations corresponds
// the number of defined expectations
func (m *HealthCheckerMock) MinimockHealthDone() bool {
	if m.HealthMock.optional {
		// Optional methods provide '0 or more' call count restriction.
		return true
	}

	for _, e := range m.HealthMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	return m.HealthMock.invocationsDone()
}

// MinimockHealthInspect logs each unmet expectation
func (m *HealthCheckerMock) MinimockHealthInspect() {
	for _, e := range m.HealthMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Error("Expected call to HealthCheckerMock.Health")
		}
	}

	afterHealthCounter := mm_atomic.LoadUint64(&m.afterHealthCounter)
	// if default expectation was set then invocations count should be greater than zero
	if m.HealthMock.defaultExpectation != nil && afterHealthCounter < 1 {
		m.t.Errorf("Expected call to HealthCheckerMock.Health at\n%s", m.HealthMock.defaultExpectation.returnOrigin)
	}
	// if func was set then invocations count should be greater than zero
	if m.funcHealth != nil && afterHealthCounter < 1 {
		m.t.Errorf("Expected call to HealthCheckerMock.Health at\n%s", m.funcHealthOrigin)
	}

	if !m.HealthMock.invocationsDone() && afterHealthCounter > 0 {
		m.t.Errorf("Expected %d calls to HealthCheckerMock.Health at\n%s but found %d calls",
			mm_atomic.LoadUint64(&m.HealthMock.expectedInvocations), m.HealthMock.expectedInvocationsOrigin, afterHealthCounter)
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *HealthCheckerMock) MinimockFinish() {
	m.finishOnce.Do(func() {
		if !m.minimockDone() {
			m.MinimockHealthInspect()
		}
	})
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *HealthCheckerMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *HealthCheckerMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockHealthDone()
}
